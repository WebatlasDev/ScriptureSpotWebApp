// app/donate/layout.tsx
// This is a placeholder layout that passes through its children
// The actual layout is controlled by the LayoutManager component
export default function DonateLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}